Explain JSON to me as a data interchange and storage format. Explain how a Python dictionary relates to a JSON object, how a Python list relates to a JSON array, and what types of Python data can and cannot be directly represented in JSON. Do not modify my project.

Explain serialization and deserialization in programming, using the above example. Explain what happens to a Python list of dictionaries when it is serialized to JSON and what happens when that JSON is loaded back into Python. Don't modify the code.

My coffee roast model uses Python date objects internally, but JSON doesn't have a native date type. Explain three reasonable approaches for serializing a Python date to JSON and converting it back. Which approach would you recommend for this small project and why?